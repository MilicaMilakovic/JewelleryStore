<UserControl x:Class="JewelleryStore.StaffControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:JewelleryStore"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="730">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="97*"/>
            <ColumnDefinition Width="124*"/>
            <ColumnDefinition Width="118*"/>
            <ColumnDefinition Width="130*"/>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="130*"/>
        </Grid.ColumnDefinitions>

        <materialDesign:PackIcon Kind="UserCircleOutline" Height="56" Foreground="#80CBC4" Width="58" Margin="0,0,22,23" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
        <Separator Background="#80CBC4" VerticalAlignment="Bottom" Grid.ColumnSpan="6"/>

        <TextBlock x:Name="nameLabel" Grid.Column="1"  Margin="7,39,6,30" />
        <Label x:Name="usernameLabel" Grid.Column="2"  Margin="7,39"/>
        <Label x:Name="sinceLabel" Grid.Column="3"  Margin="7,39"/>
        <Label x:Name="salaryLabel" Grid.Column="4"  Margin="7,39"/>
        <Button  Content="{materialDesign:PackIcon Kind=Edit}"  Margin="10"       
                 IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                 Grid.Column="5" Width="50" HorizontalAlignment="Left" />
        <Button  Content="{materialDesign:PackIcon Kind=Remove}"  Margin="10"       
                 IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                 Grid.Column="5" Width="50" HorizontalAlignment="Right" />

    </Grid>
</UserControl>
